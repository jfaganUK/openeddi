// This might be a temporary file. I'm not sure it will be what the real version will finally look like.

var app = app || {};

$(function () {
	'use strict';
	
	// This saves the slider data
	app.QuestionView.prototype.sliderFeedBackInit = function() {
		var this.model.toJSON();
		var dets = _.pluck(_.pluck(_.pluck(app.names.namesInList('list1'), 'attributes'), 'details'), 'nameinterpretSliderTest');




	};
	
});
	
